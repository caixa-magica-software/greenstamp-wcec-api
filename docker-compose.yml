version: '3'

services:

  analyzer-kadabra-api:
    container_name: analyzer-kadabra-api
    image: greenstamp/analyzer-kadabra-api
    build: 
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 3333:3000
    environment:
      DELIVER_RESULTS_ENDPOINT: ${DELIVER_RESULTS_ENDPOINT}
    volumes:
      - '/data/analyzer-kadabra-api:/data'